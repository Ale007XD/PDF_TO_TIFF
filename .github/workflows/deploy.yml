name: "CI/CD: Автоматический билд и деплой на VPS"

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  APP_NAME: pdf-to-tiff-bot

jobs:
  deploy:
    name: "Сборка и деплой на VPS"
    runs-on: ubuntu-latest

    steps:
      - name: "SSH-деплой на VPS"
        uses: appleboy/ssh-action@v1.0.3
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}  # Правильный экспорт из секретов
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e
            export BOT_TOKEN="${BOT_TOKEN}"

            APP_DIR="$HOME/apps/${{ env.APP_NAME }}"
            REPO_URL="https://github.com/${{ github.repository }}"

            echo "⏬ Обновление проекта с GitHub..."
            mkdir -p "$APP_DIR"

            if [ ! -d "$APP_DIR/.git" ]; then
              git clone "$REPO_URL" "$APP_DIR"
            else
              cd "$APP_DIR"
              git fetch origin
              git reset --hard origin/main
            fi

            cd "$APP_DIR"

            if ! command -v docker >/dev/null 2>&1; then
              echo "❌ Docker не установлен!"
              exit 1
            fi
            if ! docker compose version >/dev/null 2>&1; then
              echo "❌ 'docker compose' не найден!"
              exit 1
            fi

            echo "🛑 Остановка старого приложения..."
            docker compose down --remove-orphans || true
            # Обязательно удаляем предыдущий контейнер, если тот был зависшим
            docker rm -f pdf_to_tiff_bot || true
            echo "🧹 Очистка старых образов..."
            docker image prune -f || true

            echo "🔨 Сборка Docker-образа локально на VPS..."
            docker compose build --no-cache

            echo "🚀 Запуск приложения..."
            docker compose up -d --remove-orphans

            echo "📋 Статус контейнеров:"
            docker compose ps

            echo "📄 Логи (последние 20 строк):"
            docker compose logs --tail=20

            echo "✅ Деплой завершён!"
