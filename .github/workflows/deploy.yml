name: 'CI/CD: Автоматический билд и деплой на VPS'

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  APP_NAME: pdf-to-tiff-bot

jobs:
  deploy:
    name: 'Сборка и деплой на VPS'
    runs-on: ubuntu-latest

    steps:
    - name: 'SSH-деплой на VPS'
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        key: ${{ secrets.VPS_SSH_KEY }}
        port: ${{ secrets.VPS_PORT }}
        script: |
          set -e
          
          echo "⏬ Обновление проекта с GitHub..."
          if [ ! -d /home/${{ env.APP_NAME }} ]; then
            git clone https://github.com/${{ github.repository }} /home/${{ env.APP_NAME }}
          else
            cd /home/${{ env.APP_NAME }}
            git fetch origin
            git reset --hard origin/main
          fi

          cd /home/${{ env.APP_NAME }}

          echo "🛑 Остановка старого приложения..."
          docker compose down --remove-orphans || true

          echo "🧹 Очистка старых образов..."
          docker image prune -f

          echo "🔨 Сборка Docker-образа локально на VPS..."
          docker compose build --no-cache

          echo "🚀 Запуск приложения..."
          docker compose up -d --remove-orphans

          echo "📋 Статус контейнеров:"
          docker compose ps
          
          echo "📄 Логи:"
          docker compose logs --tail=20

          echo "✅ Деплой завершён!"
          
  health-check:
    name: 'Пост-деплой проверка'
    runs-on: ubuntu-latest
    needs: deploy
    if: success()

    steps:
    - name: 'Ожидание запуска'
      run: sleep 20

    - name: 'Health-check через curl'
      run: |
        echo "Проверяем работу приложения..."
        curl -f http://${{ secrets.VPS_HOST }}:8080/health || exit 1
        echo "✅ Приложение отвечает!"

    - name: 'Финальное уведомление'
      run: |
        echo "🚀 CI/CD завершено!"
        echo "🖥️ Хост: ${{ secrets.VPS_HOST }}"
        echo "📦 Приложение: ${{ env.APP_NAME }}"
